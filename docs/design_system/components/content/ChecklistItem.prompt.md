One-sentence: A checklist row with a rounded lime check (or muted cross) used in the "Before / With TrainerFlow" comparison panels.

```jsx
<ChecklistItem checked>Everything in one platform</ChecklistItem>
<ChecklistItem checked={false}>Hours spent on spreadsheets</ChecklistItem>
```

Props: `checked` (check vs cross), `tone` (lime/emerald).
