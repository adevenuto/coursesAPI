One-sentence: A large lime metric over a small muted label, used for proof stats.

```jsx
<StatBlock value="80%" label="Less admin time" />
<StatBlock value="10k+" label="Clients coached" tone="emerald" />
```

Props: `value`, `label`, `tone` (lime/emerald/neutral), `size` (sm/md/lg).
