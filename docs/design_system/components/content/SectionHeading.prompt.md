One-sentence: The standard section header — eyebrow badge, two-line display title with an accent-colored second line, and a muted lead.

```jsx
<SectionHeading
  eyebrow={<Badge tone="lime">The Solution</Badge>}
  title="One platform."
  highlight="Unlimited potential."
  lead="TrainerFlow brings your whole coaching workflow into one place."
/>
```

Props: `eyebrow`, `title`, `highlight` (accent second line), `lead`, `align` (center/left), `highlightTone` (lime/emerald).
