One-sentence: The dark charcoal surface container behind every panel, feature tile and dashboard widget.

```jsx
<Card glow hover>…</Card>
```

Props: `glow` (emerald top radial glow for featured cards), `hover` (lifts + lime border), `padding`. 16px radius, hairline border, soft shadow by default.
